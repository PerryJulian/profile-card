const size = {
    sm: `375px`,
    lg: `1440px`
}

const device = {
    sm: `only screen and (min-width: ${size.sm})`,
    lg: `only screen and (min-width: ${size.lg})`
}

export default {size, device}